package model;

public enum ServerResponse {
   LOGIN_SUCCESSS, ACCOUNT_CREATED, INCORRECT_USERNAME, INCORRECT_PASSWORD,
   MAX_ATTEMPTS, LOGGED_IN, LOGOUT_SUCCESS, PASSWORD_CHANGED, DOCUMENT_CREATED,
   DOCUMENT_DELETED, PERMISSION_ADDED, PERMISSION_DENIED
}
